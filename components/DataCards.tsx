import { cn } from '@/lib/utils';

interface DataCardProps { label: string; value: string | number; subtext?: string; icon?: string; }
function DataCard({ label, value, subtext, icon }: DataCardProps) { return <div className="panel group p-4 transition-colors hover:border-accent/50 hover:bg-card-hover"><div className="flex items-start justify-between gap-3"><div><p className="eyebrow">{label}</p><p className="mt-3 font-mono text-2xl font-semibold tracking-tight text-foreground">{value}</p>{subtext && <p className="mt-1 text-xs text-muted">{subtext}</p>}</div>{icon && <span className="font-mono text-xs text-accent">{icon}</span>}</div></div>; }
export default function DataCards({ cards, className }: { cards: DataCardProps[]; className?: string }) { return <div className={cn('grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-4', className)}>{cards.map((card, i) => <div key={card.label} className="animate-fade-in" style={{ animationDelay: `${i * 70}ms` }}><DataCard {...card} /></div>)}</div>; }
